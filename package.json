{
  "name": "logoquiz-multiplayer",
  "version": "1.0.0",
  "description": "Logoquiz game but multiplayer",
  "main": "app.js",
  "scripts": {
    "test": "npm run build && node server.js",
    "start": "nodemon --watch src --exec 'npm run build && node server.js'",
    "build": "babel src -d public/js"
  },
  "author": "Yesh Chandiramani",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^0.8.7",
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "express-session": "^1.14.0",
    "js-cookie": "^2.1.2",
    "lib": "^0.1.1",
    "mongoose": "^4.5.10",
    "socket.io": "^1.4.8",
    "socketio": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0",
    "babel-watch": "^2.0.2"
  }
}
